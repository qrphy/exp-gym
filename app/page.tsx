import Image from "next/image";

export default function Home() {
  return (
    <div className="flex items-center justify-center bg-inherit font-sans">
      <main className="flex w-full max-w-3xl flex-col items-center justify-between min-h-screen text-black bg-white dark:bg-black dark:text-white">
      <p>görsel içerik</p>
      </main>
    </div>
  );
}
